{
  "name": "@cher-ami/chersite",
  "description": "Web base Boilerplate for cher-ami projects.",
  "version": "2.1.0",
  "author": "cher-ami",
  "private": true,
  "scripts": {
    "dev:api": "turbo run dev --filter api",
    "dev:front": "pnpm --filter ./packages/front run dev",
    "test:front": "pnpm --filter ./packages/front run test",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.7.0"
  }
}
