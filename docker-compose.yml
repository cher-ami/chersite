version: "3.7"
services:
  node:
    image: node:20.11
    ports:
      - "${DOCKER_NODE_PORT:-5173}:5173"
      - "24678:24678" # vite hot reload port
      - "1234:1234" # expose node server for generating pages
    volumes:
      - "./:/app"
    working_dir: /app
    tty: true
