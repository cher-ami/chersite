import React from 'react';
import css from './%%upperComponentName%%.module.less';
import { cls } from "@cher-ami/utils"
import debug from "@cher-ami/debug";

interface IProps {
  className?: string
}

const componentName = "%%upperComponentName%%";
const log = debug(`front:${componentName}`);

/**
 * @name %%upperComponentName%%
 */
function %%upperComponentName%% (props: IProps) {
  return <div className={cls(css.root, props.className)}>
      {componentName}
  </div>
}

export default %%upperComponentName%%
